commands:
  install: npm install
  build: npm run build
  test: npm test
  typecheck: npx tsc --noEmit
  lint: npm run lint
  dev: npm run dev
  format: npm run format
  formatcheck: npm run format:check

services: {}
